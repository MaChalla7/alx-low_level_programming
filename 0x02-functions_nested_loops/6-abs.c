#include "main.h"
/**
 * _abs - shows the absolute value of an integer
 * @j: the integer to be computed
 * Return: absolute value
 */

int _abs(int j)
{
if (j >= 0)
{
return (j);
}
else
{
return (-j);
}
}
